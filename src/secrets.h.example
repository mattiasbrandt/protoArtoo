// =============================================================================
// src/secrets.h.example
//
// Create src/secrets.h via one of:
//   - make setup-wifi  (recommended; masked prompts)
//   - cp src/secrets.h.example src/secrets.h, then edit manually
// secrets.h is gitignored  --  never commit it.
// =============================================================================

#pragma once

// WiFi AP mode password (min 8 characters)
#define PA_AP_PASSWORD "changeme123"

// WiFi STA mode (optional  --  leave empty to skip STA connection)
#define PA_STA_SSID ""
#define PA_STA_PASSWORD ""
