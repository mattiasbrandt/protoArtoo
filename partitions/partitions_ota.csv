# Partition table for protoArtoo — 4MB ESP32 flash, OTA + LittleFS + coredump
#
# Redesigned for arduino-esp32 v3.x (IDF 5.x) which produces ~15% larger
# binaries than v2.x due to expanded network stack and USB support.
#
# App partitions: 1.625MB (0x1A0000) each. Builds land 1.49-1.57MB depending on
# framework pool state, so headroom is 57-141KB (3.5-9%), not the ~25% this line
# claimed before 2026-09-07. Anything that adds code to the image -- a second
# driver in a Component Family, say -- is measured against that, not assumed.
# LittleFS reduced from 704KB to 640KB to carve a 64KB coredump partition. The
# image costs far more than its files add up to: LittleFS rounds every file up to
# a 4KB block, so on main at 0b55e00f its 40 files (299,320 B of content, of which
# 242,546 B is gzipped text) allocate 97 blocks = 397,312 B, leaving 258,048 B.
# The "~151KB" this line claimed until 2026-09-11 counted neither the rounding nor
# the UI's growth. tools/check_build_budgets.py now measures it every build
# (#382, ADR 0065); do not read littlefs.bin's own size, which is always the full
# partition and says nothing about usage.
# Total: exactly 4MB.
#
# coredump (64KB, type data / subtype coredump): the arduino-esp32 framework
# enables CONFIG_ESP_COREDUMP_ENABLE_TO_FLASH + DATA_FORMAT_ELF + CHECK_BOOT, but
# the table previously had no coredump partition (boot logged "esp_core_dump_flash:
# No core dump partition found!"), so a PANIC left only a reset reason. With this
# partition a crash saves an ELF coredump, fetched in-PCB over HTTP via
# /api/coredump (USB read is GPIO15-blocked when seated) and decoded with:
#   esp-coredump info_corefile -c coredump.bin .pio/build/<env>/firmware.elf
# (issue #8). Changing the partition table requires a full USB flash + uploadfs;
# a plain OTA does not rewrite the table.
#
# Offsets are explicit to prevent silent layout shifts on toolchain upgrades.
#
# Name,   Type, SubType,  Offset,    Size, Flags
nvs,      data, nvs,      0x9000,  0x5000,
otadata,  data, ota,      0xe000,  0x2000,
app0,     app,  ota_0,   0x10000, 0x1A0000,
app1,     app,  ota_1,  0x1B0000, 0x1A0000,
spiffs,   data, spiffs, 0x350000,  0xA0000,
coredump, data, coredump, 0x3F0000, 0x10000,
