# Partition table for protoArtoo P4 — 16MB ESP32-P4 flash, OTA + LittleFS + coredump
#
# Layout: NVS (20KB) + OTA data (8KB) + App0 (3MB) + App1 (3MB) + LittleFS (9.88MB) + Coredump (64KB).
# LittleFS: 9.88MB (0x9E0000). One data/ image is built for every environment, so
# this partition holds what the 4MB board's 640KB one holds -- until each file
# declares which environments carry it (#382, ADR 0065). Measured, not assumed:
# tools/check_build_budgets.py images the filesystem and counts allocated blocks
# on every build. littlefs.bin's own size is always the full partition and is
# never the measurement.
# Coredump: 64KB (0x10000, type data / subtype coredump).
#
# The arduino-esp32 framework enables CONFIG_ESP_COREDUMP_ENABLE_TO_FLASH +
# DATA_FORMAT_ELF + CHECK_BOOT, so a PANIC saves an ELF coredump, fetched in-PCB over
# HTTP via /api/coredump (USB read is GPIO15-blocked when seated) and decoded with:
#   esp-coredump info_corefile -c coredump.bin .pio/build/<env>/firmware.elf
#
# Offsets are explicit to prevent silent layout shifts on toolchain upgrades.
# No gaps: each partition ends exactly where the next begins, and coredump ends at 0x1000000.
# Verification: 0xE000 (nvs+otadata) + 0x600000 (both apps) + 0x9E0000 (spiffs) + 0x10000 (coredump) = 0x1000000 (16 MB).
#
# Name,   Type, SubType,  Offset,    Size, Flags
nvs,      data, nvs,      0x9000,  0x5000,
otadata,  data, ota,      0xe000,  0x2000,
app0,     app,  ota_0,   0x10000, 0x300000,
app1,     app,  ota_1,  0x310000, 0x300000,
spiffs,   data, spiffs, 0x610000, 0x9E0000,
coredump, data, coredump, 0xFF0000, 0x10000,
