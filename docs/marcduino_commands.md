# Marcduino Commands (Legacy Filename)

This filename is kept for compatibility with older links.

The canonical command reference is:

- `docs/commands.md`

Use that file for supported command surfaces (HTTP API, manual command routing,
dome RX command handling, and RC bindable actions).
