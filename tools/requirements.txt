# tools/requirements.txt — developer tools for protoArtoo.
# Install with: pip install -r tools/requirements.txt
#
# pyserial: required for tools/serial_monitor.py (make monitor).
pyserial>=3.5

# questionary: interactive prompts for tools/configure.py (make setup).
# Falls back to plain input() if not installed — not required for core workflow.
questionary>=2.1.1

# PyYAML: required for action catalog generation from docs/action-registry.yaml.
PyYAML>=6.0.3

# rich: required by tools/soak.py. A hard dependency, not optional -- the
# harness imports it at module scope so there is one presentation path rather
# than two that can disagree about what a run looked like. It owns terminal
# detection, NO_COLOR and TERM=dumb; the harness does not reimplement any of
# that. Provided by pacman as python-rich on this project's machine.
rich>=13.0
