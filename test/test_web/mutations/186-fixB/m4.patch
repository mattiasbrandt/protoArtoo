diff --git a/data/setup.js b/data/setup.js
index aca4f28..cf1afd2 100644
--- a/data/setup.js
+++ b/data/setup.js
@@ -1427,7 +1427,7 @@
     }
     if (availabilityReason) availabilityReason.textContent = stateReason;
     if (availabilityLamp) {
-      availabilityLamp.className = `feature-availability-lamp-indicator feature-state-${result.state}`;
+      availabilityLamp.className = ""; /*MUTATION*/ void (`feature-availability-lamp-indicator feature-state-${result.state}`;
     }
     if (content) {
       content.inert = !result.available;
