diff --git a/data/shell.js b/data/shell.js
--- a/data/shell.js
+++ b/data/shell.js
@@ -1099,8 +1099,7 @@
   const refusedAt = (event) => {
     const target = event.target;
     if (!target?.closest) return null;
-    const hit = target.closest('[aria-disabled="true"]');
-    if (hit) return hit;
+    return target.closest('[aria-disabled="true"]');
     const { clientX, clientY } = event;
     if (typeof clientX !== "number" || typeof clientY !== "number") return null;
     const candidates = target.querySelectorAll?.('[aria-disabled="true"]') || [];
